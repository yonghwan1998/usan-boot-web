spring:
  datasource:
    url: jdbc:mysql://localhost:3306/usan?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true
  thymeleaf:
    cache: false
server:
  port: 8080