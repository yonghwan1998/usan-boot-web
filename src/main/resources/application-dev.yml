spring:
  datasource:
    url: jdbc:mysql://localhost:3306/${DB_NAME:mydb}?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:pw}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true